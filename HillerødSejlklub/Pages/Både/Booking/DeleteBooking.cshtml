@page
@model HillerødSejlklub.Pages.Både.Booking.DeleteBookingModel
@{
}

<div class="container mt-5">
    <h1 class="mb-4 text-danger">Sletning</h1>
    <div class="alert alert-danger">
        <h5>Er du sikker på at slette booking for <strong>@Model.Booking.MedlemToBook.Navn</strong> til båden <strong>@Model.Booking.BoatToBook.Navn</strong>?</h5>
        <form method="post" class="mt-3">
            <input type="hidden" asp-for="BookingId" />
            <button type="submit" class="btn btn-danger me-2">
                <i class="bi bi-check-circle"></i> Ja
            </button>
            <a class="btn btn-primary" asp-page="AllBookings" asp-route-bådNavn="@Model.Booking.BoatToBook.Navn">
                <i class="bi bi-x-circle"></i> Nej
            </a>
        </form>

    </div>
</div>
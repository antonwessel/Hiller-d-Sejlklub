@page
@using ClassLibrary.Core.Helpers
@model HillerødSejlklub.Pages.Boats.AllBoatsModel
@{
}

<div class="container mt-5">
    <h1 class="mb-4 text-center">Vores Både</h1>
    @if (AdminState.IsAdminLoggedIn)
    {
        <a class="btn btn-success btn-lg mb-4" asp-page="AddBoat">
            <i class="bi bi-plus-circle"></i> Tilføj en ny båd
        </a>
    }
    <div class="row">
        @foreach (var boat in Model.Boats)
        {
            <div class="col-md-4 mb-4">
                <div class="card shadow-sm border-0">
                    <img src="@boat.ImageURL" class="card-img-top rounded-top" alt="@boat.BoatName" style="height: 200px; object-fit: cover;">
                    <div class="card-body text-center">
                        <h5 class="card-title text-primary">@boat.BoatName</h5>
                        <p class="text-muted mb-4">
                            <strong>BoatType:</strong> @boat.BoatType<br />
                            <strong>Model:</strong> @boat.BoatModel
                        </p>
                        @if (AdminState.IsAdminLoggedIn)
                        {
                            <a class="btn btn-outline-primary btn-sm me-3 mb-2" asp-page="EditBoat" asp-route-boatName="@boat.BoatName" title="Rediger">
                                <i class="bi bi-pencil"></i> Rediger
                            </a>
                            <a class="btn btn-outline-danger btn-sm me-3 mb-2" asp-page="DeleteBoat" asp-route-boatName="@boat.BoatName" title="Slet">
                                <i class="bi bi-trash"></i> Slet
                            </a>
                        }
                        <a class="btn btn-outline-primary btn-sm me-3 mb-2" asp-page="Maintenance/MaintenanceBoat" asp-route-boatName="@boat.BoatName" title="Maintenance">
                            <i class="bi bi-wrench"></i> Vedligeholdelse
                        </a>
                        <a class="btn btn-outline-primary btn-sm me-3 mb-2" asp-page="Booking/AllBookings" asp-route-boatName="@boat.BoatName" title="Booking">
                            <i class="bi bi-calendar-check"></i> Booking
                        </a>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
